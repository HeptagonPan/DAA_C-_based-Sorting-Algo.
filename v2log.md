# v2 更新记录（相对 v1）

- 增加交互模式：支持选择数据集类型与规模，保留演示数据集模式。
- 数据集生成更稳健：`n < 2` 的近乎有序生成直接返回，避免无效分布。
- 输出表格更清晰：列宽自适应；被跳过的算法显示为 `skipped`。
- “实际最佳”判定更稳定：加入 `epsilon` 处理时间平局。
- 快速排序更鲁棒：使用三数取中选主元并改为迭代实现，降低最坏情况与递归深度风险。
- 文档更新：完善 `sort_benchmark.md` 说明并新增 `README.md`。
